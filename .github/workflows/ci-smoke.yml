name: CI Smoke Test

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/ci-smoke.yml"

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Confirm GitHub Actions startup
        run: echo "GitHub Actions startup works"
