name: Cloudflare Pages Diagnostic

on:
  pull_request:
    branches:
      - abos-factory/v1

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm GitHub Actions startup
        run: echo "GitHub Actions started successfully"
