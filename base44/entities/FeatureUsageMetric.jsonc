{
  "name": "FeatureUsageMetric",
  "type": "object",
  "properties": {
    "ab_variant": {
      "type": "string",
      "description": "A/B variant or whitelisted AI action name"
    },
    "conversion_count": {
      "type": "number",
      "default": 0,
      "description": "Number of conversions (goal achieved)"
    },
    "feature_name": {
      "type": "string",
      "description": "Feature being measured"
    },
    "interaction_count": {
      "type": "number",
      "default": 0,
      "description": "Number of user interactions with feature"
    },
    "measured_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this metric was recorded"
    },
    "metric_type": {
      "type": "string",
      "description": "Type of metric being tracked",
      "enum": [
        "adoption_rate",
        "engagement",
        "conversion",
        "error_rate",
        "performance",
        "user_satisfaction",
        "ai_action"
      ]
    },
    "metric_value": {
      "type": "number",
      "description": "Measured value, count, duration, score, or AI action cost"
    },
    "sample_size": {
      "type": "number",
      "default": 0,
      "description": "Number of users sampled for this metric"
    },
    "statistical_significance": {
      "type": "number",
      "default": 0,
      "description": "P-value (0.0-1.0) for A/B test significance",
      "maximum": 1,
      "minimum": 0
    },
    "tier": {
      "type": "string",
      "description": "Subscription or experiment tier"
    },
    "user_segment": {
      "type": "string",
      "description": "User segment or customer email for private usage accounting"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional description for search visibility and SEO"
    }
  },
  "required": [
    "feature_name",
    "metric_type",
    "measured_at"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "super_admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "super_admin"
      }
    }
  }
}