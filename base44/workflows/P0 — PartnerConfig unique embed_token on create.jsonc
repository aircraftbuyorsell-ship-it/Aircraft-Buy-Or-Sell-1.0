{
  "name": "P0 \u2014 PartnerConfig unique embed_token on create",
  "description": "P0 guard \u2014 when a PartnerConfig is created, if another exists with the same embed_token, keep the oldest config and delete the duplicate.",
  "trigger": {
    "condition": null,
    "config": {
      "trigger_type": "entity",
      "events": [
        "create"
      ],
      "entity_name": "PartnerConfig"
    }
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "p0_partnerconfig_unique_embed_token_on_create",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_function": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "enforceUniqueOnCreate",
            "args": {}
          },
          "then": "end"
        }
      }
    ]
  }
}