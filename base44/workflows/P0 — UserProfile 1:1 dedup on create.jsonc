{
  "name": "P0 \u2014 UserProfile 1:1 dedup on create",
  "description": "P0 guard \u2014 when a UserProfile is created, if another exists with the same user_email, merge into the oldest (highest role/tier, union permissions) and delete the duplicate.",
  "trigger": {
    "condition": null,
    "config": {
      "trigger_type": "entity",
      "events": [
        "create"
      ],
      "entity_name": "UserProfile"
    }
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "p0_userprofile_1_1_dedup_on_create",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_function": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "enforceUniqueOnCreate",
            "args": {}
          },
          "then": "end"
        }
      }
    ]
  }
}