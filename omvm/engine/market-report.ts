/**
 * OMVM 2.0 Market Report
 * Pure formatter: converts a valuation result into an auditable report payload.
 * It deliberately stores no market observations.
 */
export interface MarketReportInput { aircraft:{registration?:string;make:string;model:string;year:number}; valuation:{estimated_value_usd:number;posterior_sigma_usd:number;valuation_mode:string;confidence:{cv:number;within_10_pct:number;within_20_pct:number;within_30_pct:number;mode?:string};market_evidence?:{observation_count:number;effective_sample_size:number;evidence_type:string;market_value_usd?:number;market_sigma_log?:number};audit_trace?:{model_decisions:string[];aircraft_class?:string;valuation_mode_reason?:string}}; }
export interface MarketReport { report_version:'omvm-market-report-2.0';generated_at:string;aircraft:MarketReportInput['aircraft'];valuation:MarketReportInput['valuation'];interpretation:{headline:string;range_low_usd:number;range_high_usd:number;confidence_statement:string};evidence_policy:{asking_prices_only:boolean;persistent_comps:boolean;note:string}; }
export function buildMarketReport(input:MarketReportInput):MarketReport { const v=input.valuation.estimated_value_usd,s=Math.max(0,input.valuation.posterior_sigma_usd),low=Math.max(0,Math.round(v-1.96*s)),high=Math.round(v+1.96*s),pct=(input.valuation.confidence.within_20_pct*100).toFixed(0); return {report_version:'omvm-market-report-2.0',generated_at:new Date().toISOString(),aircraft:input.aircraft,valuation:input.valuation,interpretation:{headline:`Estimated market value: $${Math.round(v).toLocaleString('en-US')}`,range_low_usd:low,range_high_usd:high,confidence_statement:`${pct}% posterior probability within ±20% of the estimate`},evidence_policy:{asking_prices_only:true,persistent_comps:false,note:'Live marketplace observations are runtime evidence; OMVM does not maintain a persistent comps database.'}}; }
