import { FIELD_GROUPS } from "@/lib/billOfSale";
const COLORS = { verified: "text-emerald-600", likely: "text-amber-600", manual_required: "text-red-500" };
export default function ReviewTable({ draft }) {
  const rows = FIELD_GROUPS.flatMap((group) => group.fields.map(([field, label]) => ({ field, label, group: group.title })));
  return <section className="overflow-hidden rounded-2xl border border-border bg-card"><div className="border-b border-border p-5"><h2 className="font-bold text-card-foreground">Smart review</h2><p className="text-sm text-muted-foreground">Verify every legal value and its source before export.</p></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/50 text-xs text-muted-foreground"><tr><th className="px-4 py-3">Field</th><th className="px-4 py-3">Value</th><th className="px-4 py-3">Source</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{rows.map(({ field, label, group }) => { const meta = draft.field_confidence?.[field] || { source: "Manual input", level: "manual_required" }; return <tr key={field} className="border-t border-border"><td className="px-4 py-3"><span className="block text-[10px] text-muted-foreground">{group}</span>{label}</td><td className="px-4 py-3 font-medium text-card-foreground">{draft[field] || "Missing"}</td><td className="px-4 py-3 text-muted-foreground">{meta.source}</td><td className={`px-4 py-3 font-bold capitalize ${COLORS[meta.level]}`}>{meta.level.replace("_", " ")}</td></tr>; })}</tbody></table></div></section>;
}