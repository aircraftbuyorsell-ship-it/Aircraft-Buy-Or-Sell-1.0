import { Brain, Loader2, Plus, Trash2 } from "lucide-react";

export default function ConversationSidebar({ conversations, activeId, loading, onNew, onSelect, onDelete }) {
  return <aside className="hidden w-64 shrink-0 flex-col border-r border-border bg-card md:flex"><div className="border-b border-border p-4"><div className="flex items-center gap-2"><div className="flex h-8 w-8 items-center justify-center rounded-lg bg-gold-bg text-gold"><Brain className="h-4 w-4" /></div><div><p className="text-xs font-black text-foreground">Finance Advisor</p><p className="text-[9px] text-muted-foreground">Digital Twin intelligence</p></div></div></div><button onClick={onNew} className="mx-3 mb-2 mt-3 flex items-center justify-center gap-1 rounded-xl border border-gold/30 bg-gold-bg px-3 py-2 text-xs font-bold text-gold"><Plus className="h-3.5 w-3.5" /> New conversation</button><div className="flex-1 overflow-y-auto px-2 pb-4">{loading ? <div className="flex justify-center py-8"><Loader2 className="h-4 w-4 animate-spin text-muted-foreground" /></div> : conversations.map(conv => <div key={conv.id} className={`group mb-1 flex items-center rounded-xl ${activeId === conv.id ? "bg-muted" : "hover:bg-muted/60"}`}><button onClick={() => onSelect(conv)} className="min-w-0 flex-1 px-3 py-2.5 text-left"><p className="truncate text-xs font-semibold text-foreground">{conv.metadata?.name || "Untitled analysis"}</p><p className="mt-0.5 text-[9px] text-muted-foreground">{conv.messages?.length || 0} messages</p></button><button onClick={() => onDelete(conv.id)} className="mr-2 hidden rounded p-1.5 text-muted-foreground hover:bg-destructive/10 hover:text-destructive group-hover:block" aria-label="Delete conversation"><Trash2 className="h-3.5 w-3.5" /></button></div>)}</div></aside>;
}