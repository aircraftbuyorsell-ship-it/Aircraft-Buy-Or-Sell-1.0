import { Menu, Plus, Trash2, X } from "lucide-react";

export default function MobileConversationDrawer({ open, conversations, onClose, onNew, onSelect, onDelete }) {
  return <><button onClick={() => onClose(true)} className="rounded-lg p-2 text-muted-foreground" aria-label="Open conversations"><Menu className="h-5 w-5" /></button>{open && <div className="fixed inset-0 z-50 md:hidden"><button className="absolute inset-0 bg-black/50" onClick={() => onClose(false)} aria-label="Close conversations" /><section className="absolute inset-x-0 bottom-0 max-h-[75dvh] rounded-t-3xl border border-border bg-card p-4 shadow-2xl"><header className="mb-3 flex items-center justify-between"><p className="text-sm font-black text-foreground">Conversations</p><button onClick={() => onClose(false)} className="rounded-lg p-2 text-muted-foreground"><X className="h-5 w-5" /></button></header><button onClick={() => { onNew(); onClose(false); }} className="mb-3 flex w-full items-center justify-center gap-1 rounded-xl border border-gold/30 bg-gold-bg py-2 text-xs font-bold text-gold"><Plus className="h-3.5 w-3.5" />New conversation</button><div className="max-h-[50dvh] space-y-1 overflow-y-auto">{conversations.map(conversation => <div key={conversation.id} className="flex items-center rounded-xl hover:bg-muted"><button onClick={() => { onSelect(conversation); onClose(false); }} className="min-w-0 flex-1 px-3 py-3 text-left"><p className="truncate text-xs font-semibold text-foreground">{conversation.metadata?.name || "Untitled analysis"}</p></button><button onClick={() => onDelete(conversation.id)} className="mr-2 rounded p-2 text-muted-foreground hover:text-destructive" aria-label="Delete conversation"><Trash2 className="h-4 w-4" /></button></div>)}</div></section></div>}</>;
}