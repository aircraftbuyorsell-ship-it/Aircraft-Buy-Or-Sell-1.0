/**
 * Re-exported from pipelineNavConfig — navigation is now organized by sales pipeline phases.
 * Old import paths continue to work via this re-export.
 */
export { NAV_TREE, isPathInSection } from "@/components/layout/pipelineNavConfig";