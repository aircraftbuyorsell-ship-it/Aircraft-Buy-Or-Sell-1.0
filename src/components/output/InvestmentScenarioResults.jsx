import ScenarioCard from "@/components/output/ScenarioCard";
import SensitivityChart from "@/components/output/SensitivityChart";
export default function InvestmentScenarioResults({ scenarios, assumptions }) { if(!scenarios?.base)return null; return <section className="space-y-4"><div><p className="text-[10px] font-bold uppercase text-[var(--brand-primary)]">Ownership scenarios</p><h2 className="mt-1 text-xl font-bold">Cash flow and return outlook</h2></div><div className="grid gap-4 lg:grid-cols-3"><ScenarioCard label="Downside" scenario={scenarios.downside} discountRate={assumptions?.interest_rate_pct}/><ScenarioCard label="Base case" scenario={scenarios.base} discountRate={assumptions?.interest_rate_pct}/><ScenarioCard label="Upside" scenario={scenarios.upside} discountRate={assumptions?.interest_rate_pct}/></div><SensitivityChart baseScenario={scenarios.base} assumptions={assumptions}/><p className="output-muted text-xs">Scenario estimates depend on supplied assumptions and are not financial, tax, or legal advice.</p></section>; }