import { AlertTriangle, CheckCircle2, Sparkles } from "lucide-react";
function List({ title, items, risk }) { if(!items?.length)return null; const Icon=risk?AlertTriangle:CheckCircle2; return <div><p className={`mb-2 text-[10px] font-bold uppercase ${risk?'text-red-400':'text-emerald-400'}`}>{title}</p><ul className="space-y-2">{items.map((x,i)=><li key={i} className="output-muted flex gap-2 text-xs"><Icon className="mt-0.5 h-3.5 w-3.5 shrink-0"/>{x}</li>)}</ul></div>; }
export default function OutputBriefNarrative({ brief, summary, strengths, risks, recommendation, title="Intelligence brief" }) {
  const data=typeof brief==='object'&&brief?brief:{}; const text=typeof brief==='string'?brief:(data.brief_summary||summary);
  return <section className="output-panel p-5"><div className="mb-4 flex items-center gap-2 text-[var(--brand-primary)]"><Sparkles className="h-4 w-4"/><h3 className="text-xs font-bold uppercase">{title}</h3></div>{text&&<p className="text-sm leading-relaxed text-[var(--brand-text)]">{text}</p>}<div className="mt-5 grid gap-5 sm:grid-cols-2"><List title="Strengths" items={data.strengths||strengths}/><List title="Risks" items={data.risks||risks} risk/></div>{(data.recommendation||recommendation)&&<div className="mt-5 rounded-lg border border-[var(--brand-primary)]/30 bg-[var(--brand-primary)]/10 p-3"><p className="text-[10px] font-bold uppercase text-[var(--brand-primary)]">Recommendation</p><p className="mt-1 text-sm">{data.recommendation||recommendation}</p></div>}{data.confidence_assessment&&<p className="output-muted mt-3 text-xs italic">{data.confidence_assessment}</p>}</section>;
}