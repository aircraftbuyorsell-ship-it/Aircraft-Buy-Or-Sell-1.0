import { Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
import { sensitivityRows } from "@/lib/outputFinance";
export default function SensitivityChart({ baseScenario, assumptions }) { const rows=sensitivityRows(baseScenario,assumptions); if(!rows.length)return null; return <section className="output-panel p-5"><div><p className="text-[10px] font-bold uppercase text-[var(--brand-primary)]">IRR sensitivity</p><h3 className="mt-1 text-lg font-bold">What moves the investment case</h3><p className="output-muted mt-1 text-xs">Change from base-case IRR when each assumption moves independently.</p></div><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={rows} layout="vertical" margin={{left:8,right:20}}><CartesianGrid stroke="#2F374A" horizontal={false}/><XAxis type="number" tick={{fill:'#8A94A6',fontSize:10}} unit=" pts"/><YAxis type="category" dataKey="factor" width={135} tick={{fill:'#F3F4F6',fontSize:10}}/><Tooltip formatter={v=>`${v} IRR pts`} contentStyle={{background:'#151922',border:'1px solid #2F374A',fontSize:11}}/><Legend wrapperStyle={{fontSize:11}}/><Bar dataKey="down" name="Lower case" fill="#EC5D5D" radius={[0,4,4,0]}/><Bar dataKey="up" name="Higher case" fill="#5DCAA5" radius={[0,4,4,0]}/></BarChart></ResponsiveContainer></div></section>; }