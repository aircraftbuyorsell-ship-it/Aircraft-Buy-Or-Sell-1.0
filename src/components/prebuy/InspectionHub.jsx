import { Plus, Plane, ShieldCheck } from 'lucide-react';
import CreateInspectionForm from './CreateInspectionForm';
import InspectionStatus from './InspectionStatus';
export default function InspectionHub({ inspections, creating, setCreating, onCreate, busy, onOpen }) {
  return <main className="mx-auto max-w-6xl px-4 py-8 md:px-8"><div className="mb-7 flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-gold">ABOS Inspection Workspace</p><h1 className="mt-2 text-3xl font-black text-foreground">Aircraft pre-buy, built for the whole deal team.</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Buyer-led collaboration with certified inspection, evidence, AI risk analysis, immutable reporting, and sales pipeline handoff.</p></div><button onClick={()=>setCreating(!creating)} className="flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 font-bold text-primary-foreground"><Plus className="h-4 w-4"/>New inspection</button></div>
    {creating&&<div className="mb-7"><CreateInspectionForm onCreate={onCreate} busy={busy}/></div>}
    {!inspections.length&&!creating?<div className="rounded-3xl border border-dashed border-border p-14 text-center"><ShieldCheck className="mx-auto h-10 w-10 text-gold"/><h2 className="mt-4 text-xl font-bold">No inspection workspaces yet</h2><p className="mt-2 text-sm text-muted-foreground">Create the first workspace and invite your inspection team.</p></div>:<div className="grid gap-4 md:grid-cols-2">{inspections.map(i=><button key={i.id} onClick={()=>onOpen(i)} className="rounded-2xl border border-border bg-card p-5 text-left transition hover:border-gold/50"><div className="flex items-start justify-between"><Plane className="h-5 w-5 text-gold"/><InspectionStatus status={i.status}/></div><h2 className="mt-4 text-xl font-black">{i.aircraft?.registration}</h2><p className="text-sm text-muted-foreground">{i.aircraft?.year} {i.aircraft?.make} {i.aircraft?.model}</p><p className="mt-4 text-xs text-muted-foreground">{i.report_id} · v{i.version} · {i._viewer_role}</p></button>)}</div>}
  </main>;
}