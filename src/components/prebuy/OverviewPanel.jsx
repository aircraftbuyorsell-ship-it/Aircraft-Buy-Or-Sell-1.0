import MediaUploader from './MediaUploader';
export default function OverviewPanel({ report, onMedia }) {
  const complete=(report.checklist||[]).filter(i=>i.status!=='pending').length,total=report.checklist?.length||0;
  return <div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5 md:col-span-2"><p className="text-xs font-bold uppercase text-gold">Aircraft</p><h2 className="mt-2 text-2xl font-black">{report.aircraft?.registration}</h2><p className="text-muted-foreground">{report.aircraft?.year} {report.aircraft?.make} {report.aircraft?.model}</p><div className="mt-6 grid grid-cols-3 gap-3"><div><b className="text-2xl">{complete}/{total}</b><p className="text-xs text-muted-foreground">Checklist</p></div><div><b className="text-2xl">{report.findings?.length||0}</b><p className="text-xs text-muted-foreground">Findings</p></div><div><b className="text-2xl">{report.media?.length||0}</b><p className="text-xs text-muted-foreground">Evidence</p></div></div></div><div className="rounded-2xl border border-border bg-card p-5"><h3 className="font-bold">Evidence vault</h3><p className="mt-1 text-xs text-muted-foreground">Photos, videos, maintenance records and supporting documents.</p><div className="mt-4"><MediaUploader onUploaded={onMedia}/></div><div className="mt-4 space-y-2">{(report.media||[]).slice(-5).map((m,i)=><a key={`${m.url}-${i}`} href={m.url} target="_blank" rel="noreferrer" className="block truncate text-xs text-gold">{m.name}</a>)}</div></div></div>;
}