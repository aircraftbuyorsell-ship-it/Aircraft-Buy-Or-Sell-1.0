import { Download, LockKeyhole, Sparkles } from 'lucide-react';
import { exportPreBuyPdf } from '@/lib/prebuyPdf';
export default function ReportPanel({ report, onAnalyze, onFinalize, onRevision, busy }) {
  const canAnalyze=['buyer','inspector','admin'].includes(report._viewer_role); const canFinalize=['inspector','admin'].includes(report._viewer_role);
  return <div className="space-y-4"><div className="flex flex-wrap gap-2">{report.status!=='finalized'&&canAnalyze&&<button onClick={onAnalyze} disabled={busy} className="flex items-center gap-2 rounded-xl border border-gold/40 px-4 py-2 font-bold text-gold"><Sparkles className="h-4 w-4"/>Generate AI analysis</button>}{report.status!=='finalized'&&canFinalize&&<button onClick={onFinalize} disabled={busy} className="flex items-center gap-2 rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground"><LockKeyhole className="h-4 w-4"/>Sign & finalize</button>}{report.status==='finalized'&&['buyer','inspector','admin'].includes(report._viewer_role)&&<button onClick={()=>exportPreBuyPdf(report)} className="flex items-center gap-2 rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground"><Download className="h-4 w-4"/>Export branded PDF</button>}{report.status==='finalized'&&['buyer','admin'].includes(report._viewer_role)&&<button onClick={onRevision} className="rounded-xl border border-border px-4 py-2 font-bold">Create new version</button>}</div><section className="rounded-2xl border border-border bg-card p-6"><div className="flex justify-between gap-4"><div><p className="text-xs font-bold text-gold">{report.report_id} · VERSION {report.version}</p><h2 className="mt-2 text-2xl font-black">Inspection Report</h2></div><span className="text-sm font-bold capitalize">{report.verification_status?.replaceAll('_',' ')}</span></div><h3 className="mt-6 font-bold">Executive summary</h3><p className="mt-2 whitespace-pre-wrap text-sm text-muted-foreground">{report.executive_summary||'Generate analysis when findings are ready.'}</p><h3 className="mt-5 font-bold">Risk assessment</h3><p className="mt-2 whitespace-pre-wrap text-sm text-muted-foreground">{report.risk_assessment||'Pending'}</p><div className="mt-6 rounded-2xl border border-gold/30 bg-gold/10 p-5"><p className="text-xs font-bold uppercase text-gold">Buyer guidance</p><p className="mt-2 text-3xl font-black">{report.decision_guidance}</p><p className="mt-2 text-sm text-muted-foreground">{report.recommended_next_actions}</p></div></section></div>;
}