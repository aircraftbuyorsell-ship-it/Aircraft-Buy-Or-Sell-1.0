import { useState } from "react";
import { Button } from "@/components/ui/button";

export default function SellerIntakeForm({ onSubmit, loading }) {
  const [form, setForm] = useState({ registration: "", make: "", model: "", year: "", asking_price: "" });
  const update = (key, value) => setForm({ ...form, [key]: value });
  return <form className="space-y-4" onSubmit={(e) => { e.preventDefault(); onSubmit(form); }}><div><label htmlFor="registration" className="mb-2 block text-sm font-semibold">N-number</label><input id="registration" value={form.registration} onChange={(e) => update("registration", e.target.value.toUpperCase())} placeholder="N123AB" className="w-full rounded-lg border px-3 py-3" /></div><div className="grid grid-cols-2 gap-3"><div><label htmlFor="make" className="mb-2 block text-sm font-semibold">Make</label><input id="make" required={!form.registration} value={form.make} onChange={(e) => update("make", e.target.value)} className="w-full rounded-lg border px-3 py-3" /></div><div><label htmlFor="model" className="mb-2 block text-sm font-semibold">Model</label><input id="model" required={!form.registration} value={form.model} onChange={(e) => update("model", e.target.value)} className="w-full rounded-lg border px-3 py-3" /></div></div><div className="grid grid-cols-2 gap-3"><div><label htmlFor="year" className="mb-2 block text-sm font-semibold">Year</label><input id="year" type="number" value={form.year} onChange={(e) => update("year", e.target.value)} className="w-full rounded-lg border px-3 py-3" /></div><div><label htmlFor="price" className="mb-2 block text-sm font-semibold">Asking price</label><input id="price" required type="number" min="0" value={form.asking_price} onChange={(e) => update("asking_price", e.target.value)} placeholder="USD" className="w-full rounded-lg border px-3 py-3" /></div></div><Button type="submit" disabled={loading} className="w-full bg-primary text-primary-foreground">{loading ? "Připravuji report…" : "Vytvořit Listing Readiness Report"}</Button></form>;
}