{
  "schema_version": "1.0.0",
  "status": "canonical_draft",
  "canonical_contract": {
    "openapi": "3.1.0",
    "base_path": "/api/v1",
    "response_envelope": "operation_specific",
    "error_schema": "APIError",
    "authentication": {
      "accepted_transports": ["Authorization: Bearer <ABOS API key>", "X-ABOS-API-Key: <key>"],
      "api_keys_in_body_forbidden": true,
      "dual_credential_policy": "reject",
      "precedence": null
    },
    "operations": [
      { "method": "POST", "path": "/api/v1/search", "operation_id": "searchListings", "scope": "search:read" },
      { "method": "GET", "path": "/api/v1/aircraft/{aircraft_id}", "operation_id": "getAircraft", "scope": "listings:read" },
      { "method": "GET", "path": "/api/v1/listings/{listing_id}", "operation_id": "getListing", "scope": "listings:read" },
      { "method": "POST", "path": "/api/v1/intelligence/valuate", "operation_id": "requestValuation", "scope": "intelligence:request" }
    ]
  },
  "legacy_gateway": {
    "method": "POST",
    "path": "/functions/abosCoreApi",
    "api_key_header": "x-abos-key",
    "status": "compatibility_only",
    "sdk_exposure": false,
    "mcp_exposure": false,
    "operations": ["keys.create", "keys.list", "keys.revoke", "search", "valuate", "intelligence.extract", "listings.get", "listings.list", "listings.create"]
  },
  "compatibility_mappings": [
    { "legacy": "search", "canonical": "POST /api/v1/search", "mode": "future_adapter", "notes": "Preserve the deployed legacy path until a separately tested adapter reaches parity." },
    { "legacy": "valuate", "canonical": "POST /api/v1/intelligence/valuate", "mode": "separate_during_coexistence", "notes": "Legacy keeps its current provider and response. Canonical valuation independently enforces provenance and insufficient_data." },
    { "legacy": "listings.get", "canonical": "GET /api/v1/listings/{listing_id}", "mode": "future_adapter", "notes": "Resolve internal ID to stable public listing_id only after parity tests exist." },
    { "legacy": "listings.list", "canonical": "POST /api/v1/search", "mode": "future_adapter", "notes": "Translate structured filters only after parity tests exist." },
    { "legacy": "keys.create", "canonical": null, "mode": "legacy_only", "notes": "Defer API-key lifecycle contract." },
    { "legacy": "keys.list", "canonical": null, "mode": "legacy_only", "notes": "Defer API-key lifecycle contract." },
    { "legacy": "keys.revoke", "canonical": null, "mode": "legacy_only", "notes": "Defer API-key lifecycle contract." },
    { "legacy": "intelligence.extract", "canonical": null, "mode": "legacy_only", "notes": "Requires a separate reviewed capability contract." },
    { "legacy": "listings.create", "canonical": null, "mode": "legacy_only", "notes": "Requires ownership, authorization and idempotency design." }
  ],
  "invariants": {
    "public_ids_only": true,
    "internal_ids_forbidden": true,
    "deterministic_retrieval": true,
    "source_provenance_required": true,
    "valuation_without_traceable_sources": "insufficient_data",
    "sanitized_errors": true,
    "request_id_required": true,
    "legacy_gateway_must_remain_available_during_coexistence": true,
    "legacy_behavior_must_not_change_in_task001": true,
    "complete_valuation_requires_nonempty_traceable_provenance": true
  },
  "scope_migration": {
    "listing:read": { "canonical": "listings:read", "legacy_alias_only": true },
    "intelligence:read": { "canonical": "intelligence:request", "legacy_alias_only": true },
    "search:read": { "canonical": "search:read", "legacy_alias_only": false },
    "listing:write": { "canonical": null, "legacy_alias_only": true }
  }
}
